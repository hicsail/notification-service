{
  "name": "@hicsail/notification",
  "version": "0.0.1",
  "description": "The notifications microservice provides a common abstraction layer on top of the various notification types and actual sending.",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prettier": "npm run prettier --workspaces --if-present",
    "prettier:fix": "npm run prettier:fix --workspaces --if-present",
    "start": "npm run start --workspaces --if-present",
    "start:dev": "npm run start:dev --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "type":"module",
  "workspaces": [
    "./packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hicsail/notification-service.git"
  },
  "author": "Boston University Software & Application Innovation Lab <hicsail@bu.edu> (https://sail.bu.edu/)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/hicsail/notification-service/issues"
  },
  "homepage": "https://github.com/hicsail/notification-service#readme",
  "dependencies": {
    "aws-sdk": "^2.1227.0",
    "dotenv": "^16.0.3",
    "uuid": "^9.0.0"
  }
}
